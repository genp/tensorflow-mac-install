# Install TF for Mac MX
Run 
```
chmod +x tf-install.sh
./tf_install.sh
```
To create a virtual env with tensorflow available on a machine with Apple SIlicon. 
Change the venv name in the install script.